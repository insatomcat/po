[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "goose61850"
version = "0.1.0"
description = "Librairie Python pour envoyer, recevoir et analyser des messages GOOSE (IEC 61850-8-1)."
authors = [
  { name = "Auto-generated by Cursor AI" }
]
requires-python = ">=3.9"
dependencies = [
  "scapy>=2.5.0",
]

[project.urls]
Homepage = "https://example.com/goose61850"
